var idValue = document.getElementById("validationCustom01").value;
var requestValue = document.getElementById("validationCustom02").value;
var responseValue = document.getElementById("validationCustom03").value;
var typeValue = document.getElementById("validationCustom04").value;
var vatNumberValue = document.getElementById("validationCustom05").value;
var businessUnitValue = document.getElementById("validationCustom06").value;
var tcrValue = document.getElementById("validationCustom07").value;
var aedIdValue = document.getElementById("validationCustom08").value;
var createdAtValue = document.getElementById("validationCustom09").value;
var statusValue = document.getElementById("validationCustom10").value;
var lastRetryValue = document.getElementById("validationCustom11").value;
var retryNrValue = document.getElementById("validationCustom12").value;
var extraFieldsValue = document.getElementById("validationCustom13").value;

function createClient() {
    const faturaData = {
        id: idValue,
        request: requestValue,
        response: responseValue,
        type: typeValue,
        vatnumber: vatNumberValue,
        businessunit: businessUnitValue,
        tcr: tcrValue,
        aedid: aedIdValue,
        createdat: createdAtValue,
        status: statusValue,
        lastretry: lastRetryValue,
        retrynr: retryNrValue,
        extrafields: extraFieldsValue,
    };

    fetch("/createClients", {
        method: "POST",
        headers: {
            "Content-Type": "application/json",
        },
        body: JSON.stringify(faturaData),
    })
        .then((response) => response.json())
        .then((data) => {
            console.log(data.message)
        })
        .catch((error) => {
            ongoingRequests.delete(id); // Remove the request flag on error
            checkDataTable("");
        });
}

//...
